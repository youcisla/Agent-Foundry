# Cursor AI Rules — Agent Foundry
# Place this in .cursor/rules/agent-foundry.mdc

# Use Agent Foundry when the user:
# - invokes /af or /plan
# - pastes a multi-step task that needs planning
# - asks to "audit" or "review" code with structured criteria
# - needs cross-file refactors

# Always prefer the Agent Foundry skills over hand-rolled heuristics.
# The 30 skills in skills/core/ cover prompt-discipline, plan-before-code,
# verify-first, anti-slop, etc.
